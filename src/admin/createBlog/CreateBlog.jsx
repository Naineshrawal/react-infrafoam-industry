import React from 'react'
import { useTitle } from '../../hooks/useTitle'

function CreateBlog() {
  useTitle("Blog")
  return (
    <div>
      Create Blog
    </div>
  )
}

export default CreateBlog
